# --- Voice Live (Foundry resource hosting a realtime model) ---
# API keys may be disabled by policy -> prefer Entra ID. Leave AZURE_VOICE_LIVE_API_KEY
# empty and authenticate with `az login` (local) or a managed identity (in Azure).
# The signed-in identity needs a data-plane role on the resource
# (e.g. "Cognitive Services User" / "Foundry User").
AZURE_VOICE_LIVE_ENDPOINT=https://<your-foundry-resource>.cognitiveservices.azure.com/
AZURE_VOICE_LIVE_API_KEY=
# Voice Live uses the native model name (e.g. gpt-realtime), not a deployment name.
VOICE_LIVE_MODEL=gpt-realtime
# VOICE_LIVE_API_VERSION=2026-04-10
# AZURE_USER_ASSIGNED_IDENTITY_CLIENT_ID=   # optional, for user-assigned managed identity

# --- Energy Fraud Detection API (the dashboard backend) ---
FRAUD_API_BASE_URL=https://<your-dashboard>.azurecontainerapps.io
# FRAUD_API_SUBSCRIPTION_KEY=   # only if you front it with APIM

# --- Azure Communication Services (optional: phone calls + email summaries) ---
ACS_CONNECTION_STRING=
ACS_DEV_TUNNEL=
ACS_SENDER_EMAIL=
